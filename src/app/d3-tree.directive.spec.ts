import { D3TreeDirective } from './d3-tree.directive';

describe('D3TreeDirective', () => {
  it('should create an instance', () => {
    const directive = new D3TreeDirective();
    expect(directive).toBeTruthy();
  });
});
